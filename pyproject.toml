[project]
name = "lungai-diagnostics"
version = "0.1.0"
description = "Explainable chest X-ray classification research prototype"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
